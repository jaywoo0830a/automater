"""
cli
---
JSON/YAML-driven campaign execution — no DB required.

Parallel composition root alongside factory (DB-driven).
Both converge on automator's PostingSpec → JobRunner → BlogEditor pipeline.
"""
