services:
  api:
    build:
      context: ..
      dockerfile: docker/api.Dockerfile
    network_mode: host
    env_file:
      - ../.env
    volumes:
      - ../workspaces:/app/workspaces
      - ../config:/app/config

  web:
    build:
      context: ..
      dockerfile: docker/web.Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - api
